# 实施计划

1. 在 `QQGateway` 增加 Gateway URL 缓存、受控重连结果/延迟和可中断退避等待。
2. 调整 `run()`，区分受控重连与需要刷新 URL 的错误，避免每轮重置为 1 秒并重复调用 `/gateway`。
3. 在 `QQBotAdapter` 增加生命周期串行化与幂等启动，确保 reload 不产生重复连接。
4. 增加 Gateway 和 adapter 生命周期回归测试，覆盖 100017 的触发路径。
5. 运行相关测试、完整测试及静态检查；根据结果修正实现。
6. 检查是否需要将此次连接生命周期约束补充到 `.trellis/spec/backend`，然后准备提交。
